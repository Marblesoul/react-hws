/** MapWidget отвечает за блок карты и связанных городских ссылок. */
const MapWidget = () => (
  <div className="map-widget">
    <a href="#map">Расписания</a>
    <a href="#map">Такси</a>
    <a href="#map">Афиша</a>
  </div>
);

export default MapWidget;
